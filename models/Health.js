const db = require(`./db`);

class Health {
  constructor(id, dailyscore, date, notes) {
    this.id = id,
      this.dailyscore = dailyscore,
      this.date = date,
      this.notes = notes
  }

  // CREATE


  // RETRIEVE


  // UPDATE


  // DELETE
}

module.exports = Health;